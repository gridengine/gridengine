#!/bin/sh

BUILD_ARCH=$1

rm -rf $BUILD_ARCH
mkdir $BUILD_ARCH
cd $BUILD_ARCH

if [ $BUILD_ARCH = "SOLARIS64" ]; then
   CFLAGS="-fast -xarch=v9"
   export CFLAGS
fi   
../configure --srcdir=.. --prefix=`pwd` --enable-shared --with-included-gettext
make install
